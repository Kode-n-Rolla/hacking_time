# Tips

- User may have 2 access key. Check count and try to add the second acces key to backdoor, for example.
- After get a access keys, check (with `aws cli` and/or `pacu`)
    - whoami
    - roles
    - groups
    - policies (attached, inline)
    - permissions
