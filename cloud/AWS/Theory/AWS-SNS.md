# What is it?

SNS (Simple Notification Service) - это **сервис отправки уведомлений и сообщений**. Он позволяет отправлять сообщения на:

- 📬 **Email**
- 📱 **SMS**
- ☁️ **HTTP/S endpoints (вебхуки)**
- 🔁 **SQS (Simple Queue Service)**
- 🔥 **Lambda-функции**

# 🤖 Как это работает?

1. Создаётся **топик** (topic) — как канал рассылки.
2. Сюда подписываются **подписчики (subscribers)**: email, endpoint, очередь и т.д.
3. Кто-то вызывает `Publish`, и SNS рассылает сообщение всем подписчикам.

# ⚠️ Где тут уязвимости?

Если SNS **неправильно настроен**, злоумышленник может:

- 📩 Подписать **свой email или вебхук** на топик
- 🕵️ Получить чувствительные данные из публикаций (например, логи, флаги, токены)
- 🔄 Использовать подписку как канал обратной связи (например, для C2 или SSRF)
